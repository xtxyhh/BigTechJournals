import { PremiumErrorState } from "@/components/RouteFeedback";

export default function NotFound() {
  return <PremiumErrorState title="Internship page not found" description="That internship destination is not available yet." />;
}
