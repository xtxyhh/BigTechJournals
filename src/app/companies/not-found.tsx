import { PremiumErrorState } from "@/components/RouteFeedback";

export default function NotFound() {
  return <PremiumErrorState title="Company guide not found" description="That company guide is not available yet." />;
}
